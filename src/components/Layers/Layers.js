import React from "react";

/*
* This component is a placeholder to put all the layers.
*/
const Layers = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default Layers;