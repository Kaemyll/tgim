import React from "react";

/*
* This component is a placeholder to put the map controls.
*/
const Controls = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
};

export default Controls;